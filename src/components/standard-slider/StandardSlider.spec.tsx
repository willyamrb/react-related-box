import React from "react";
import { render } from "@testing-library/react";

import StandardSlider from "./StandardSlider";

describe("StandardSlider", () => {
  test("renders the StandardSlider component", () => {
    render(<StandardSlider />);
  });
});
